export default function WaitlistContainer({ children }: { children: React.ReactNode }) {
  return (
    <div className="px-4 sm:px-6 lg:px-10 xl:px-14 max-w-[1220px] mx-auto">
      {children}
    </div>
  );
}
